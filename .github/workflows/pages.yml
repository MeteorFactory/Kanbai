name: Kanbai — Website

on:
  push:
    branches: [main]
    paths:
      - 'website/**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    uses: MeteorFactory/Pipelines/.github/workflows/deploy-pages.yml@main
    with:
      path: website
